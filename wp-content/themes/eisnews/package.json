{
  "name": "eisnews",
  "version": "1.0.0",
  "description": "",
  "main": "tailwind.config.js",
  "scripts": {
    "css": "tailwindcss -i ./src/tailwind.css -o ./src/style.css --watch --jit",
    "minify": "tailwindcss -i ./src/tailwind.css -o ./src/style.css --minify --jit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
